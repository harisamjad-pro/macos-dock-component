import { motion } from "framer-motion";

drag
                dragConstraints={{
                    top: -100,
                    left: -100,
                    right: 100,
                    bottom: 100,
                }}





                motion.div

                initial={{ x: -80 }}
                animate={{ x: 0 }}
                transition={{ duration: 0.2 }}
                whileHover={{ scale: 1.04 }}
                whileTap={{ scale: 0.96 }}